@{
    ViewData["Title"] = "SignIn";
}
@model SignInViewModel

@section Scripts
{
    @await Html.PartialAsync("_ValidationScriptsPartial");
}

<div class="row">
    <div class="col-md-6 offset-md-3">
        <h2 class="text-center mb-3">Kullanıcı Giriş</h2>

        <div asp-validation-summary=ModelOnly class="alert alert-danger" role="alert">
            @TempData["SuccessMessage"]
        </div>

        <form asp-controller="Home" asp-action="SignIn" asp-route-returnurl="@Context.Request.Query["returnURL"]" method="post">
            <div class="mb-3">
                <label for="Email" class="form-label" asp-for="Email"></label>
                <input value="enes@enes.com" class="form-control" asp-for="Email">
                <span class="text-danger" asp-validation-for="Email"></span>
            </div>

            <div class="mb-3">
                <label for="Password" class="form-label" asp-for="Password"></label>
                <input value="Password12*" type="password" class="form-control" asp-for="Password">
            </div>

            <div class="row">
                <div class="col-md-6">
                    <div class="mb-3 form-check">
                        <input type="checkbox" class="form-check-input" asp-for="RememberMe">
                        <label class="form-check-label" for="RememberMe" asp-for="RememberMe"></label>
                    </div>
                </div>
                <div class="col-md-6 text-end">
                    <div>
                        <a class="btn btn-link no-underline move-up" asp-controller="Home" asp-action="ResetPassword">Şifremi unuttum</a>
                    </div>
                </div>
            </div>

            <div class="row">
                <button type="submit" class="btn btn-primary">Üye Giriş</button>

            </div>

        </form>
    </div>
</div>

